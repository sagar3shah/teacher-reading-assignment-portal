spring.application.name=backend
server.port=${PORT:8080}

# Comma-separated list of allowed origins for browser requests (for deployed frontend).
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
